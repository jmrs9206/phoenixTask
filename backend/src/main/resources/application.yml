spring:
  application:
    name: phoenixtask-backend
  profiles:
    default: local

server:
  port: ${BACKEND_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
